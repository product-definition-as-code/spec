# Case: structured-behaviour-uses-terms-removed

**Verifies:** removing a Structured Behaviour `uses-terms` target emits one overlay `PRODUCT024`.

**Spec references:**

- [Product Changes](../../../spec/product-changes.md#overlay-validation) - a removal-caused dangling relationship is `PRODUCT024`.

## Expected

`PRODUCT024` is attributed to the baseline Structured Behaviour's `uses-terms` relationship.

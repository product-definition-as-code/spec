{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:product-definition-as-code:schema:citation-sidecar:v1alpha2",
  "title": "Citation sidecar ledger",
  "description": "Canonical adjacent YAML sidecar for one consumer document. The corresponding consumer file and YAML feature restrictions are defined normatively in the Citation Contract.",
  "type": "object",
  "additionalProperties": false,
  "required": ["citations"],
  "properties": {
    "citations": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "urn:product-definition-as-code:schema:common:v1alpha2#/$defs/citationRecord"
      }
    }
  }
}
